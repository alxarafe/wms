meta {
  name: Leer politica picking
  type: http
  seq: 47
}

get {
  url: {{base_url}}/api/warehouses/{{a_id}}/location-types/{{picking_id}}/hu-policies
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("Leer politica picking", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.deep.equal({hu_policies: [bru.getVar("picking_policy_record")]});
  });
}
