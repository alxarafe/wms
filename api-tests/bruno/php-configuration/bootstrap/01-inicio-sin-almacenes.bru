meta {
  name: Inicio sin almacenes
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/warehouses
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("Inicio sin almacenes", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.deep.equal({warehouses: []});
  });
}
