meta {
  name: Java Health Check
  method: GET
  url: {{java_base_url}}/api/health
}

headers {
  Accept: application/json
}

assert {
  res.status: eq 200
  res.body.status: eq ok
  res.body.timestamp: isDefined
}
