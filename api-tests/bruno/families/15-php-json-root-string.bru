meta {
  name: PHP - json-root-string
  type: http
  seq: 15
}

post {
  url: {{php_base_url}}/api/item-families
  body: none
  auth: none
}

headers {
  Content-Type: application/json
  Accept: application/json
}

script:pre-request {
  req.setBody("json-root-string", { raw: true });
}

tests {
  test("rechaza JSON sin objeto raíz", function () {
    expect(res.getStatus()).to.equal(400);
    expect(res.getBody().error).to.equal("Invalid JSON body.");
  });
}
