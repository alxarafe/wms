meta {
  name: PHP - location-attribute
  type: http
  seq: 12
}

post {
  url: {{php_base_url}}/api/item-families
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Accept: application/json
}

body:json {
  {
    "code": "INVALID_FAMILY",
    "name": "Atributo de ubicación",
    "attributes": [
      "COLD"
    ]
  }
}

tests {
  test("rechaza location-attribute", function () {
    expect(res.getStatus()).to.equal(400);
    expect(res.getBody().error).to.equal("Unknown FAMILY attribute: COLD");
  });
}
