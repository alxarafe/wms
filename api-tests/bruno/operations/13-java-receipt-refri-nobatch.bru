meta {
  name: Java - entrada REFRI sin lote
  type: http
  seq: 13
}

post {
  url: {{java_base_url}}/api/receipts
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Accept: application/json
}

body:json {
  {
    "locationId": "01a0aca9-bc00-7020-8000-000000000008",
    "itemCode": "REFRI",
    "quantity": 30,
    "unit": "EA"
  }
}

tests {
  test("Java exige lote para REFRI", function () {
    expect(res.getStatus()).to.equal(400);
  });
}