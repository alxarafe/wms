meta {
  name: Java - salida completa 200
  type: http
  seq: 17
}

post {
  url: {{java_base_url}}/api/issues
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Accept: application/json
}

body:json {
  {
    "locationId": "01a0aca9-bc00-7020-8000-000000000008",
    "itemCode": "REFRI",
    "quantity": 30,
    "unit": "EA"
  }
}

tests {
  test("Java desocupa por completo el hueco con la salida total", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().code).to.equal("P-A-04-02");
    expect(res.getBody().references.length).to.equal(0);
  });
}