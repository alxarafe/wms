meta {
  name: Java - salida con unidad incorrecta 400
  type: http
  seq: 16
}

post {
  url: {{java_base_url}}/api/issues
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Accept: application/json
}

body:json {
  {
    "locationId": "01a0aca9-bc00-7020-8000-000000000008",
    "itemCode": "REFRI",
    "quantity": 30,
    "unit": "KG"
  }
}

tests {
  test("Java valida la unidad frente al stock almacenado", function () {
    expect(res.getStatus()).to.equal(400);
  });
}