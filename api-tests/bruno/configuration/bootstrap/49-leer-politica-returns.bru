meta {
  name: Leer politica returns
  type: http
  seq: 49
}

get {
  url: {{base_url}}/api/warehouses/{{a_id}}/location-types/{{returns_id}}/hu-policies
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("Leer politica returns", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.deep.equal({hu_policies: [bru.getVar("returns_policy_record")]});
  });
}
