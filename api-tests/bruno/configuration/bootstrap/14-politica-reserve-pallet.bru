meta {
  name: Politica reserve pallet
  type: http
  seq: 14
}

post {
  url: {{base_url}}/api/warehouses/{{a_id}}/location-types/{{reserve_id}}/hu-policies
  body: json
  auth: none
}

headers {
  Accept: application/json
  Content-Type: application/json
}

body:json {
  {
    "handling_unit_type_id": "{{pallet_id}}",
    "accepts_full": true,
    "allows_full_dispatch": true
  }
}

tests {
  test("Politica reserve pallet", function () {
    expect(res.getStatus()).to.equal(201);
    const data = res.getBody();
    const expected = {
      "location_type_id": bru.getVar("reserve_id"),
      "handling_unit_type_id": bru.getVar("pallet_id"),
      "accepts_full": true,
      "accepts_partial": false,
      "allows_breakdown": false,
      "allows_full_dispatch": true
    };
    
    expect(data).to.deep.equal(expected);
    bru.setVar("reserve_policy_record", data);
  });
}
