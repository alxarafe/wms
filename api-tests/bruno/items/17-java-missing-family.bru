meta {
  name: JAVA - familia inexistente
  type: http
  seq: 17
}

post {
  url: {{java_base_url}}/api/items
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Accept: application/json
}

body:json {
  {
    "sku": "BIDON",
    "name": "Bidón de 20 L",
    "familyCode": "FAMX",
    "baseUomCode": "BOX",
    "isBatchManaged": false,
    "isExpirable": false
  }
}

tests {
  test("rechaza una familia inexistente", function () {
    expect(res.getStatus()).to.equal(400);
    expect(res.getBody().error).to.equal("Item family code does not exist.");
  });
}